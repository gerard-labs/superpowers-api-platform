---
description: Build API Platform 4.3 filters with the modern parameters + QueryParameter pattern (never #[ApiFilter] or AbstractFilter)
---

Use the `gerard:api-platform-filters` skill exactly as written.

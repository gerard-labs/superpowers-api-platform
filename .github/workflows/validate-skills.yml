name: Validate Skills

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  validate:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Validate skills structure
        run: npx --yes tsx scripts/validate_skills.ts

      - name: Anti-regression lint (legacy API Platform patterns + structural checks)
        run: npx --yes tsx scripts/lint_skill_content.ts

      - name: Check skill naming convention
        run: |
          echo "Checking skill naming conventions..."

          # Skill `name:` frontmatter must be bare (no namespace prefix) —
          # Claude Code applies the plugin namespace automatically from plugin.json.
          find skills -name "SKILL.md" | while read skill_file; do
            name=$(grep -A1 "^---" "$skill_file" | grep "^name:" | sed 's/name: //')

            if [[ -n "$name" && "$name" =~ : ]]; then
              echo "ERROR: $skill_file has invalid name: $name (must NOT contain a namespace prefix)"
              exit 1
            fi
          done

          echo "All skills follow naming convention!"

      - name: Validate plugin.json
        run: |
          echo "Validating plugin.json..."

          # Check required fields exist
          if ! jq -e '.name' .claude-plugin/plugin.json > /dev/null 2>&1; then
            echo "ERROR: plugin.json missing 'name' field"
            exit 1
          fi

          if ! jq -e '.description' .claude-plugin/plugin.json > /dev/null 2>&1; then
            echo "ERROR: plugin.json missing 'description' field"
            exit 1
          fi

          echo "plugin.json is valid!"

      - name: Validate command files
        run: |
          echo "Validating command files..."

          for cmd_file in commands/*.md; do
            if [[ -f "$cmd_file" ]]; then
              # Check if file has frontmatter
              if ! head -1 "$cmd_file" | grep -q "^---"; then
                echo "ERROR: $cmd_file missing frontmatter"
                exit 1
              fi

              # Check if description exists in frontmatter
              if ! grep -q "^description:" "$cmd_file"; then
                echo "ERROR: $cmd_file missing 'description' in frontmatter"
                exit 1
              fi
            fi
          done

          echo "All command files are valid!"

      - name: Validate agent files
        run: |
          echo "Validating agent files..."
          if [ -d "agents" ]; then
            for agent_file in agents/*.md; do
              if [[ -f "$agent_file" ]]; then
                if ! head -1 "$agent_file" | grep -q "^---"; then
                  echo "ERROR: $agent_file missing frontmatter"
                  exit 1
                fi
                if ! grep -q "^name:" "$agent_file"; then
                  echo "ERROR: $agent_file missing 'name' in frontmatter"
                  exit 1
                fi
                if ! grep -q "description:" "$agent_file"; then
                  echo "ERROR: $agent_file missing 'description' in frontmatter"
                  exit 1
                fi
              fi
            done
            echo "All agent files are valid!"
          else
            echo "No agents directory (optional)"
          fi
